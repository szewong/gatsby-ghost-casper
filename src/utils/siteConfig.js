module.exports = {
    siteUrl: `https://www.szewong.com`, // Site domain. Do not include a trailing slash!

    postsPerPage: 12, // Number of posts shown on paginated pages (changes this requires sometimes to delete the cache)

    siteTitleMeta: `Boostraping Million-dollar Companies`, // This allows an alternative site title for meta data for pages.
    siteDescriptionMeta: `Boostraping Million-dollar Companies`, // This allows an alternative site description for meta data for pages.

    shareImageWidth: 1000, // Change to the width of your default share image
    shareImageHeight: 523, // Change to the height of your default share image

    shortTitle: `SzeWong`, // Used for App manifest e.g. Mobile Home Screen
    siteIcon: `avatar-60.png`, // Logo in /static dir used for SEO, RSS, and App manifest
    backgroundColor: `#493f3f`, // Used for Offline Manifest
    themeColor: `#15171A`, // Used for Offline Manifest
}
